#!/usr/bin/env bash
set -eou pipefail











apk add -U bash bash-completion vim coreutils postgres-client postfix mutt tzdata \
  && mkdir -p /out \
  && cp /usr/share/zoneinfo/America/Los_Angeles /etc/localtime \
  && echo "America/Los_Angeles" >/etc/timezone \
  && rm -rf /var/cache/apk/*
